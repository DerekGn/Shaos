@page
@using Shaos.Sdk
@model Shaos.Pages.PlugInInstances.ConfigurationModel

<h1>Configuration</h1>

<hr />

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @{
                if (Model.Configuration != null)
                {
                    var properties = Model.Configuration.GetType().GetProperties();

                    foreach (var property in properties)
                    {
                        var propertyValue = @property
                        .GetValue(@Model.Configuration, null);
                        var propertyAttribute = @property
                        .GetCustomAttributes(false)
                        .FirstOrDefault(_ => _.GetType() == typeof(PlugInConfigurationSettingAttribute)) as PlugInConfigurationSettingAttribute;

                        <div class="form-group">
                            <label asp-for="@property.Name" class="control-label">@property.Name</label>
                            <input asp-for="@propertyValue" name="Configuration.Name" id="Configuration.Name" class="form-control" title="@propertyAttribute.Description" />
                        </div>
                    }
                }
            }
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-sm btn-success" />
                <a asp-page="./Index" class="btn btn-sm btn-primary">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}